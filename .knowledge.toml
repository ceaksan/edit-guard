[project]
name = "edit-guard"
type = "saas"

[content]
extensions = [".md"]

[code]
preset = "python"
paths = ["hooks", "tests", "benchmark"]

[graph]
enabled = false

[indexing]
ignore_patterns = ["__pycache__", ".venv", "*.egg-info", ".git"]
